const Package = ({ City, OrderId, Status }) => {
    return (
        <>
            <h1>{City}</h1>
            <h2>{OrderId}</h2>
            <h3>{Status}</h3>
        </>
    );
}

export default Package;
